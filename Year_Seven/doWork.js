/**
 * @param {(number)[]} inbox
 */
function humanResourceMachine(inbox) {
  let outbox = [];
  let floor = [];
  outbox = inbox.filter(element => element != 0); 
  return outbox;
}


export default humanResourceMachine;
